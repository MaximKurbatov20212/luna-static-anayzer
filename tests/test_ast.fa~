import c_func(int, real, name, value, string) as func;
import c_func1(int, real, name, value, string) as func;
import c_init(int, name) as init;

sub main() {
	df x, y;
	init(1, x);	

	func(1, 1.0, y, x, "hello");
}
